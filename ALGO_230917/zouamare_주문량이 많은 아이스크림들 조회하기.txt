SELECT FLAVOR
FROM (
SELECT  FLAVOR, SUM(TOTAL_ORDER) as TOTAL_ORDER
FROM    (SELECT *
         FROM FIRST_HALF
         UNION ALL
         SELECT *
         FROM JULY)
GROUP BY FLAVOR
ORDER BY TOTAL_ORDER DESC
)
WHERE ROWNUM <= 3
;